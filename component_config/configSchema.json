{
  "type": "object",
  "title": "Parameters",
  "required": [
    "api_url",
    "#token"
  ],
  "properties": {
    "api_url": {
      "type": "string",
      "title": "API URL",
      "propertyOrder": 100,
      "description": "The URL, where the Magento API sits."
    },
    "#token": {
      "type": "string",
      "format": "password",
      "title": "Token",
      "description": "Authorization token to Magento API.",
      "propertyOrder": 300
    }
  }
}